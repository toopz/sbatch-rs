[package]
name = "sbatch-rs"
version = "0.1.0"
edition = "2021"

[dependencies]
derive_more = {version="1.0.0", features=["display", "from_str"]}
once_cell = "1.20.2"
regex = "1.11.1"
thiserror = "2.0.11"

[dev-dependencies]
rstest = "0.24.0"
